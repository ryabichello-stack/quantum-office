# P1.1 — Landing site version (canonical)

**Revision:** 2026-09-01  
**Статус:** **v2 — owner-approved** · v4 **rejected as default**

---

## Решение владельца (2026-09-01)

> Версия **v4 hero** на `/` **не принимается**. Предыдущая **v2 landing** значительно лучше.  
> **Не переключать** default landing на v4 без явного одобрения владельца.

---

## Что где

| URL | Версия | Назначение |
|-----|--------|------------|
| **`/`** (dlno.ru, staging `/delno/`) | **v2** | **Canonical marketing landing** — `DelnoPage` без `version4` |
| **`/v4`** | v4 | Эксперимент / reference only — **не prod default** |
| **`/v2`** | v2 | Явный alias того же v2 layout |

**Код:** `DELNO-site-v23/app/page.tsx` → `<DelnoPage />` (без prop `version4`).

---

## v2 vs v4 (кратко)

| | v2 (canonical) | v4 (not default) |
|---|----------------|------------------|
| Hero | «Клиенты пишут и звонят. DELNO отвечает.» | «Ваш ИИ-сотрудник…» + roles strip |
| Product visual | Inbox console mockup | Animated product stage |
| CTA primary | «Попробовать голосом» | «Попробовать DELNO» |
| FAQ | Classic 8 items | v4 tariff-focused FAQ |

---

## Для агентов / ChatGPT

- **DO NOT** set `version4` on `/` or redeploy v4 as default.
- **DO NOT** treat P1.1–P1.3 as closed on v4 messaging — clarity test applies to **v2**.
- v4 code may remain at `/v4` for comparison; do not delete without owner ask.

---

## Deploy check

```bash
curl -sf https://a.47z.ru/delno/ | grep -o 'Клиенты пишут' | head -1   # v2 hero
curl -sf https://a.47z.ru/delno/ | grep -o 'Ваш ИИ-сотрудник' | head -1  # should be empty on /
```
