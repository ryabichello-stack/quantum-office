import { DelnoPage } from "./v2/page";
import "./v4/v4.css";

/** Default landing uses v4 hero clarity (ИИ-сотрудник messaging). */
export default function DelnoHome() {
  return <DelnoPage version4 />;
}
